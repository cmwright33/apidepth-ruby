name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    name: Test
    runs-on: ubuntu-latest

    strategy:
      matrix:
        ruby-version: ["3.1", "3.2", "3.3"]

    steps:
      - uses: actions/checkout@v4

      - name: Checkout collector (SSRF fixture)
        uses: actions/checkout@v4
        with:
          repository: apidepth-io/apidepth-collector
          token: ${{ secrets.GH_PAT }}
          path: apidepth-collector
          sparse-checkout: tests/fixtures
          sparse-checkout-cone-mode: false

      - uses: ruby/setup-ruby@v1
        with:
          ruby-version: ${{ matrix.ruby-version }}
          bundler-cache: true

      - name: Tests
        run: bundle exec rspec

  coverage:
    name: Coverage
    uses: apidepth-io/apidepth-tooling/.github/workflows/coverage-ruby.yml@main
    secrets: inherit
